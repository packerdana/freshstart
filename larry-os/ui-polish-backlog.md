# UI Polish Backlog (small, safe wins)

- Make primary CTA buttons consistent (radius, color, pressed state)
- Improve Today screen spacing on small phones
- Standardize card headers (font sizes + weights)
- Ensure text contrast meets accessibility (white/70 on blue, etc.)
- Add skeleton/loading placeholders for predictions
- Reduce visual noise in Setup (grouping + progressive disclosure)
- Make errors actionable (short + one button)
